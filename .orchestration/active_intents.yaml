# Machine-generated by TRP1 hook system (select_active_intent / syncActiveIntent).
# Tracks lifecycle of business requirements; post-hook updates when agent picks a task.
# Multiple intents and statuses show diverse lifecycle and cross-session evolution.
active_intents:
  - id: "INT-001"
    name: "JWT Authentication Migration"
    status: "IN_PROGRESS"
    owned_scope:
      - "src/auth/**"
      - "src/middleware/jwt.ts"
    constraints:
      - "Must not use external auth providers"
      - "Must maintain backward compatibility with Basic Auth"
    acceptance_criteria:
      - "Unit tests in tests/auth/ pass"

  - id: "INT-002"
    name: "Billing API"
    status: "DONE"
    owned_scope:
      - "src/billing/**"
      - "src/api/billing.ts"
    constraints:
      - "Idempotent payment endpoints"
    acceptance_criteria:
      - "POST /billing/charge returns 200 with idempotency key"

  - id: "INT-003"
    name: "Audit Logging"
    status: "BLOCKED"
    owned_scope:
      - "src/audit/**"
    constraints: []
    acceptance_criteria: []
