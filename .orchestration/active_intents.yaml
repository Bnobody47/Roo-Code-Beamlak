# Machine-generated by TRP1 hook system (select_active_intent / syncActiveIntent).
# Tracks lifecycle of business requirements; post-hook updates when agent picks a task.
active_intents:
  - id: "INT-001"
    name: "JWT Authentication Migration"
    status: "IN_PROGRESS"
    owned_scope:
      - "src/auth/**"
      - "src/middleware/jwt.ts"
    constraints:
      - "Must not use external auth providers"
      - "Must maintain backward compatibility with Basic Auth"
    acceptance_criteria:
      - "Unit tests in tests/auth/ pass"
